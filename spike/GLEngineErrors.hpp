#pragma once

namespace GLEngine {
	namespace Errors { // Place for all non third-party errors to be defined
		// Errors

		const char* ErrorMessage(int error)
		{
			switch (error)
			{
			}

			return "Unknown error";
		};
		
	}
}
